@model ABIS.Models.LABOR_CATEGORY

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<aside>
    <div class="panel panel-info">
        <div class="panel-heading">
            @Html.DisplayNameFor(model => model.ASDLaborCategoryID)
        </div>
        <div>
            @Html.DisplayFor(model => model.ASDLaborCategoryID)
        </div>
    </div>
    <div class="panel panel-info">
        <div class="panel-heading">
            @Html.DisplayNameFor(model => model.LaborCategory)
        </div>
        <div>
            @Html.DisplayFor(model => model.LaborCategory)
        </div>
    </div>
</aside>
@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    <p>
        <button type="submit" value="Delete" class="btn btn-primary" >Delete
        </button>
        <button class="btn btn-primary">
            @Html.ActionLink("Back to List", "CategoryList", null, new { @style = "text-decoration:none; color:white;" })   
        </button>
    </p>
}
