@model ABIS.Models.LABOR_CATEGORY

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>LABOR_CATEGORY</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.ASDLaborCategoryID)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.ASDLaborCategoryID)
            @Html.ValidationMessageFor(model => model.ASDLaborCategoryID)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.LaborCategory)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.LaborCategory)
            @Html.ValidationMessageFor(model => model.LaborCategory)
        </div>

        <p>
            <input type="submit" value="Create" class="btn btn-primary"  />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "CategoryList")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
